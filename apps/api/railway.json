{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @crm/database generate && pnpm --filter @crm/contracts build && pnpm --filter @crm/database build && pnpm --filter @crm/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @crm/api start",
    "healthcheckPath": "/v1/health",
    "healthcheckTimeout": 100
  }
}
