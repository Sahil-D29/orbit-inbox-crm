{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npx pnpm install --frozen-lockfile && npx pnpm --filter @crm/database generate && npx pnpm --filter @crm/contracts build && npx pnpm --filter @crm/database build && npx pnpm --filter @crm/web build",
  "outputDirectory": ".next",
  "installCommand": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
