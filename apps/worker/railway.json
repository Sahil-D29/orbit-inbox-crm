{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @crm/contracts build && pnpm --filter @crm/database build && pnpm --filter @crm/worker build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @crm/worker start"
  }
}
